## Data（数据定义）

静态数据定义文件，包含成就系统和节气提醒的配置数据。

**文件：**

- achievements.ts - 成就系统数据定义（成就列表、稀有度配置）
- solarTerms.ts - 24节气数据定义（诗词、现代解读、渐变色配置）

**约定：**

- 数据文件使用 TypeScript 定义，导出类型和常量
- 配置数据使用 Record 或 Map 结构便于查询
